[package]
name = "day1-blocking-echo"
version = "0.1.0"
edition = "2021"

# No dependencies. Today is std::net only — the whole point is to feel what
# blocking I/O costs before tokio takes the pain away tomorrow.
[dependencies]
