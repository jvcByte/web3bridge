//! Week 4 · Day 5 — Blog API, request handlers.
//!
//! Handlers only. No routing here — that is `routes/`. The split means you can
//! read the whole URL surface of the API in one short file without scrolling past
//! business logic.

pub mod health;
pub mod posts;
